**W3Schools - HTML Review**

GitHub reference commands

- git status
- git branch
- to travel from main to local the command is = git checkout local
- git add . (add all files to stage'to commit we will use staging')
- to commit the command is = git commit -m "publishing to github"
- git checkout main = to travel from local to main
- git merge local = it merges all local files in main branch
- git push origin = this is a command which pushes local live to github live 

10-Aug-2026 

- HTML Layouts 
- HTML Responsive  
